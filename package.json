{
  "name": "voiceup",
  "version": "1.0.0",
  "description": "AI-powered Civic Issue Tracker",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "install:backend": "cd backend && npm install",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
